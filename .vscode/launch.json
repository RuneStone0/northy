{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "purpose": ["debug-test"],
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}